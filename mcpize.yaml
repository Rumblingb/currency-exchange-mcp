# MCPize manifest - https://mcpize.com
# Generated by mcpize analyze
version: 1
name: currency-exchange-mcp
description: >-
  Real-time exchange rates for AI agents. Convert currencies, fetch rates, and discover 166 world currencies — all with
  zero API keys.
runtime: python
entry: server.py
build:
  install: pip install -r requirements.txt
startCommand:
  type: stdio
  command: python3
  args:
    - server.py
